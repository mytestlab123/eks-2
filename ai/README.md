# AI Folder

- `commit_messages/` : curated PR messages.
- `progress.md`      : dated log of agent changes.
- `issues.md`        : (top-level) working TODO/DOING/DONE list.

Authoring Notes
- Keep diffs small and focused.
- Avoid logging secrets; prefer SSM for sensitive values.

